#include "main.h"
#include <ctype.h>
/**
 * _isupper - Check descpriton
 * @c: Input char
 *  Return: 1 if the char is lower case 0 if not
 *  _isupper check if the char c lower case or not
 */
int _isupper(int c)
{
if (isupper(c))
return (1);
else
return (0);
}
