#include "main.h"
#include "string.h"
/**
 * _strcmp - compare 2 strings
 * @s1: the first string
 * @s2: the second string
 * Return: 0 if both are the same , positive if bigger , negative if smaller
 */
int _strcmp(char *s1, char *s2)
{
return (strcmp(s1, s2));
}
