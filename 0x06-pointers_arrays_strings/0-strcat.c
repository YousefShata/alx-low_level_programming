#include "main.h"
#include <string.h>
/**
 * *_strcat - concatunate 2 strings into one
 * @dest: first string
 * @src: second string
 * Return: concatunated string
 */
char *_strcat(char *dest, char *src)
{
return (strcat(dest,src));
}
